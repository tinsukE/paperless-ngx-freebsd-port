#!/bin/sh
[ "$USER" != %%USER%% ] && echo "This script should be run as %%USER%%. Aborting." && exit 1

export PAPERLESS_BIND_ADDR=0.0.0.0
export PAPERLESS_PORT=8000

. %%OPTDIR%%/venv/bin/activate
cd %%OPTDIR%%/src
exec gunicorn -c %%OPTDIR%%/gunicorn.conf.py paperless.asgi:application

